# ── Raw / large data (obtain via DATA.md; never committed) ──────────────────
Inputs/
Xenium/
Xenium.zarr/
data/
SpatialPCA-main/

# ── Heavy intermediates and scratch ─────────────────────────────────────────
tidest_tmp/
old/

# ── Generated outputs (regenerate via the Makefile) ─────────────────────────
results_raw/

# ── Paper sources & manuscript text (kept out of the public code repo) ──────
paper/
results/

# ── Scripts not used for any paper simulation or figure (kept local only) ───
# Published repo carries only the code behind the paper's simulations and
# figures (main Figs 2-5; supplementary Figs B.1, B.2, C.3, D.4).
scripts/mb_competitor_ablation.py
scripts/gbm_samples_figure.py
scripts/prepare_darmanis.py
scripts/readme.md
scripts/simulation/run_calibration.py
scripts/simulation/sim_calibration_figure.py
scripts/simulation/run_sim_stress_tests.py
scripts/simulation/sim_stress_tests_figure.py
scripts/simulation/run_edger_nb.R

# ── Python build / cache ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.ipynb_checkpoints/
.pytest_cache/

# ── OS / editor / local tooling ─────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
.claude/
